import { global } from 'stitches.config'

export const globalStyles = global({
  body: {
    backgroundColor: '$black',
    color: '$white',
    fontFamily: '$sans',
    margin: 0,
    fontSize: '$2',
    fontWeight: '$2',
    lineHeight: '$1',
    overflow: 'hidden'
  },
})